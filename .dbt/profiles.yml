gastos_publicos:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: gastos_publicos
      threads: 4
      type: postgres
      user: airflow
  target: dev
